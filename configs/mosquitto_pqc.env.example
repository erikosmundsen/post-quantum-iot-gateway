# Example configuration for the PQC Mosquitto broker.
# Copy this file to configs/mosquitto_pqc.env and edit the paths so they match your Raspberry Pi.
#
# To create the real file:
#   cp configs/mosquitto_pqc.env.example configs/mosquitto_pqc.env
#
# Then edit the certificate paths and username below.

# Port where the PQC-enabled Mosquitto broker will listen.
MOSQ_PQC_PORT=8884

# Absolute paths to your PQC CA certificate and server certificate/key.
# Replace <your-user> with your actual username on the Pi.
MOSQ_PQC_CA_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/ca/ca.crt
MOSQ_PQC_SERVER_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/server/server.crt
MOSQ_PQC_SERVER_KEY=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/server/server.key

# Mutual TLS behavior.
MOSQ_PQC_REQUIRE_CERT=true
MOSQ_PQC_IDENTITY_AS_USERNAME=true
