# Example configuration for the MQTT CLI tools.
# Copy this file to configs/mqtt_cli.env and edit the certificate paths for your Pi.
#
# To create the real file:
#   cp configs/mqtt_cli.env.example configs/mqtt_cli.env

# Broker settings used by the CLI test scripts.
MQTT_CLI_HOST=localhost
MQTT_CLI_PORT=8884
MQTT_CLI_TOPIC=team1/sensor

# Absolute paths to the CA certificate and the client certificate/key used by the CLI tools.
# Replace <your-user> with your actual username.
MQTT_CLI_CA_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/ca/ca.crt
MQTT_CLI_CLIENT_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/client/client.crt
MQTT_CLI_CLIENT_KEY=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/client/client.key
