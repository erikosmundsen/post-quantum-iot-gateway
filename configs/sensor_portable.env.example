# Example configuration for the Pi-based DHT11 sensor node.
# Copy this file to configs/sensor_portable.env and edit it for your Pi.
#
# To create the real file:
#   cp configs/sensor_portable.env.example configs/sensor_portable.env

# How the DHT11 script connects to the PQC MQTT broker.
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=8884
MQTT_TOPIC=team1/sensor
MQTT_CLIENT_ID=dht11-portable

# Absolute paths to the CA certificate and the client certificate/key used by the DHT11 node.
# Replace <your-user> with your actual username.
MQTT_CA_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/ca/ca.crt
MQTT_CLIENT_CERT=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/client/client.crt
MQTT_CLIENT_KEY=/home/<your-user>/post-quantum-iot-gateway/artifacts/tls/client/client.key

# GPIO pin used for the DHT11 sensor (BCM numbering).
DHT11_PIN=4
